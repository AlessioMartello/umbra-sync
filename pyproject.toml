[project]
name = "umbra-sync"
version = "0.1.0"
description = "Umbra Outlook-Monday.com sync"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "msal>=1.35.1",
]
